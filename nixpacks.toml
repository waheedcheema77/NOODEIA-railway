[phases.setup]
nixpkgsArchive = "b134951a4c9f3c995fd7be05f3243f8ecd65d798"
nixPkgs = ["nodejs_22", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "cd frontend && npm install --legacy-peer-deps",
    "python3 -m venv /app/.venv",
    "/app/.venv/bin/pip install -r frontend/requirements.txt"
]

[phases.build]
cmds = ["cd frontend && NODE_OPTIONS='--max-old-space-size=8192' npm run build"]

# Declare variables in the designated block so BOTH Node and Python see them!
[variables]
PYTHON_PATH = "/app/.venv/bin/python"

[start]
cmd = "cd frontend && npm start"
